[package]
authors = ["konstin <konstin@mailbox.org>"]
name = "fail-test"
version = "0.1.0"

[lib]
name = "fail_test"
crate-type = ["cdylib"]

[dependencies]
omni = { path = "../../", features = ["omni_python", "omni_ruby"] }
